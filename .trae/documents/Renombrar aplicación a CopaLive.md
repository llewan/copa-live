Voy a renombrar la aplicación a **CopaLive** y restaurar la consistencia de la marca en todos los archivos.

1. **Restaurar Componente Logo (`src/components/Logo.tsx`)**:

   * Cambiar el texto de "FIFA World Cup 2026" (o "TRAE SCORES") a **CopaLive**.

   * Diseño propuesto: "Copa" en blanco y "Live" en un color distintivo (ej. verde o rojo para indicar 'en vivo', o mantener el gris/blanco según prefieras). Asumiré un estilo estándar y limpio.

   * Mantendré el icono del balón ⚽ por ahora, a menos que tengas un icono específico en mente.

2. **Actualizar Título y Metadatos (`index.html`** **&** **`src/components/SEO.tsx`)**:

   * Cambiar `<title>` a "CopaLive - Resultados en Vivo".

   * Actualizar etiquetas meta (OpenGraph, Twitter) para reflejar el nombre "CopaLive".

3. **Actualizar Textos y Traducciones (`src/locales`)**:

   * Reemplazar referencias en los archivos de traducción (es, en, fr) para que muestren "CopaLive" en el título del dashboard y footer.

4. **Actualizar Documentación (`README.md`)**:

   * Reflejar el nombre correcto en el archivo de léame.

