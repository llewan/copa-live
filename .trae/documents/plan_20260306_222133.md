I will help you investigate the data matching issue by creating a comprehensive database analysis script and guiding you through the execution steps.

Since I do not have direct access to your production database credentials in this environment, I will create a script that you can run locally to perform the analysis.

### Plan: Database Investigation & Analysis

1.  **Create Investigation Script (`scripts/investigate-db.ts`)**
    - This script will connect to your database using the `POSTGRES_URL` or `DATABASE_URL` environment variable.
    - It will execute targeted diagnostic queries:
        - **Connectivity Check:** Verify connection to the production database.
        - **Configuration Check:** Inspect `allowed_leagues` to ensure leagues are whitelisted.
        - **Data Volume Check:** Count total records in `matches` and `leagues`.
        - **Recent Data Check:** Group matches by date for yesterday, today, and tomorrow to verify data density.
        - **Live Data Check:** Check for any currently live matches or matches with recent status updates.
        - **Sync Status:** Inspect the `sync_status` table to verify when the last successful sync occurred.
    - It will output a detailed report to the console.

2.  **Execute Investigation**
    - You will need to run this script with your production database credentials.
    - Command: `POSTGRES_URL="your_connection_string" npx tsx scripts/investigate-db.ts`

3.  **Analyze Findings**
    - Based on the script's output, we will identify if the issue is:
        - **Empty Database:** Tables are not initialized.
        - **Missing Configuration:** `allowed_leagues` is empty (blocking new data).
        - **Sync Failure:** Cron/Sync jobs are failing to run or persisting data.
        - **Data Mismatch:** Data exists but date/timezone handling is filtering it out.

4.  **Recommendations**
    - I will provide specific fixes based on the investigation results (e.g., seeding the database, adjusting sync logic, or fixing timezone queries).
