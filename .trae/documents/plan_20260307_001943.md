I will implement a **Betting Odds Integration** proof of concept. This will allow you to demonstrate the affiliate monetization potential immediately.

## Implementation Steps

### 1. Data Structure & Mocking
- **Update Match Interface:** Add an `odds` field to the `Match` type to support betting data (Home Win, Draw, Away Win).
- **Create Mock Odds Service:** Since real-time odds APIs are expensive or require approval, I will create a `mockOddsService.ts` that generates realistic odds for upcoming matches based on team rankings (e.g., higher ranked team gets lower odds). This allows you to test the UI without a paid subscription.

### 2. UI Integration
- **Match Card Update:**
  - Add a subtle "Odds" indicator on the dashboard cards for upcoming matches.
  - Display the "1X2" (Home/Draw/Away) odds in a small, clickable area that could lead to an affiliate link.
- **Match Detail Page - New "Betting" Tab:**
  - Add a dedicated **"Betting"** tab alongside "Overview", "Lineups", etc.
  - **Content:**
    - **Match Winner:** Big, clear buttons for Home/Draw/Away with odds.
    - **Insights:** Simple auto-generated tips like "Arsenal has won their last 3 games."
    - **Call to Action:** A "Place Bet" button that simulates an affiliate redirect.

### 3. Analytics (Optional but Recommended)
- Add a tracking event when a user clicks an odd, so you can measure potential revenue.

This proof of concept will make the app look "live" with commercial intent, ready for you to swap in real API data later.